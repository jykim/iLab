h3. Weights

|<%= $features.join("|") %>|RecipRank(test)|
|<%= $comb_weights[-1][0].join("|") %>|<%= $perf_test.mean %>|
|<%= $comb_weights_svm.join("|") %>|<%= $perf_test_svm.mean %>|

<% $cand_set_score.each_with_index do |cands,i| %>
h3. Query <%= i+$offset %> : <%= cands[0][0] %>

|Query|<%= $features.join("|") %>|Sum|
<%= cands.map{|c|c.to_tbl(:round_at=>3)}.join("\n") %>
<% end %>

<parameters>
<% if smoothing %>
        <scorer><%= smoothing %></scorer>
				<mu><%= mu %></mu>
				<lambda><%= lambda %></lambda>
<% end %>
<% topics.each_with_index do |query,i| %>
        <query>
					<number><%= i + query_offset %></number>
					<text><%= get_prm_gquery(query, o[:hlm_weights]) %></text>
				</query><% end  %>
</parameters>
